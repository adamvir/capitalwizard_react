// Simplified ArenaScreen for React Native
// Full implementation will be added later with complete question database
// This is a placeholder showing the structure
import React, { useState } from 'react';
import { View, Text, StyleSheet, TouchableOpacity, ScrollView } from 'react-native';
import { MaterialCommunityIcons } from '@expo/vector-icons';
import LinearGradient from 'react-native-linear-gradient';
import { COLORS, SPACING, SIZES } from '../utils/styleConstants';

export default function ArenaScreen({ navigation, route }: any) {
  const { coins } = route.params || { coins: 1000 };
  const [gameState, setGameState] = useState<'betting' | 'playing' | 'result'>('betting');

  return (
    <View style={styles.container}>
      <LinearGradient colors={['#0F172A', 'rgba(88, 28, 135, 0.2)', '#0F172A']} style={styles.gradient}>
        <View style={styles.header}>
          <TouchableOpacity onPress={() => navigation.goBack()} style={styles.backButton}>
            <MaterialCommunityIcons name="arrow-left" size={24} color="#fff" />
          </TouchableOpacity>
          <Text style={styles.headerTitle}>Küzdőtér</Text>
          <View style={styles.coinsContainer}>
            <MaterialCommunityIcons name="lightning-bolt" size={16} color="#FBBF24" />
            <Text style={styles.coinsText}>{coins}</Text>
          </View>
        </View>

        <ScrollView style={styles.content}>
          <Text style={styles.placeholderText}>Arena Screen - Coming Soon!</Text>
          <Text style={styles.descriptionText}>
            A teljes Arena implementáció hamarosan elérhető lesz a kérdésbázissal együtt.
          </Text>
        </ScrollView>
      </LinearGradient>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1 },
  gradient: { flex: 1 },
  header: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', padding: SPACING.base, marginTop: 48 },
  backButton: { width: 40, height: 40, alignItems: 'center', justifyContent: 'center' },
  headerTitle: { color: COLORS.white, fontSize: SIZES.fontXL },
  coinsContainer: { flexDirection: 'row', alignItems: 'center', gap: 4 },
  coinsText: { color: '#FDE047', fontSize: SIZES.fontSM },
  content: { flex: 1, padding: SPACING.base },
  placeholderText: { color: COLORS.white, fontSize: SIZES.fontLG, textAlign: 'center', marginTop: 100 },
  descriptionText: { color: '#94A3B8', fontSize: SIZES.fontSM, textAlign: 'center', marginTop: SPACING.base },
});
